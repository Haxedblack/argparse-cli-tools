# File Organizer (Day 6)
A powerful and intuitive CLI tool to organize files in a directory based on their extension.

## Features
- **Group files by type**: Automatically creates folders for different file types (e.g., 'Images', 'Documents', 'Videos').
- **Safe and smart**:
Won't move itself and handles existing folders gracefully.
- **Colored output** for better readability

## Example Usuage
### Organize a directory
'''bash
# Create some dummy files first to test
# touch file1.txt fiel2.pdf image1.jpg
python organizer.py --path.
